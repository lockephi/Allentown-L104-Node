# ═══════════════════════════════════════════════════════════
# L104 Sovereign Node — Python Dependencies v2.1 (Feb 2026)
# UPGRADE v2.1:
#   - Updated all dependencies to latest compatible versions
#   - anthropic 0.45.0 → 0.79.0
#   - google-genai 1.1.0 → 1.63.0
#   - structlog 24.4.0 → 25.5.0
#   - psutil 6.1.0 → 7.2.2
#   - pytest 8.0.0 → 9.0.2
#   - uvloop 0.21.0 → 0.22.1
#   - httpx 0.28.0 → 0.28.1
#   - websockets 14.0 → 15.0 (constrained by google-genai)
# UPGRADE v2.0:
#   - Removed Flask (FastAPI is the sole web framework)
#   - Removed duplicate JSON libs (kept orjson, dropped ujson)
#   - Moved optional deps to requirements-optional.txt
#   - Added pytest + httpx test deps
#   - Pinned both floor and ceiling versions
# ═══════════════════════════════════════════════════════════

# Web framework & ASGI
fastapi>=0.115.7,<1.0
uvicorn[standard]>=0.34.2,<1.0
pydantic>=2.12.1,<3.0
jinja2>=3.1.6,<4.0
python-multipart>=0.0.20

# HTTP client (HTTP/2 enabled)
httpx[http2]>=0.28.1,<1.0

# AI providers
anthropic>=0.79.0,<1.0
google-genai>=1.63.0,<2.0

# System monitoring
psutil>=7.2.2

# Performance: async event loop + fast JSON
uvloop>=0.22.1;sys_platform!='win32'
orjson>=3.10.16

# Networking
websockets>=14.0,<15.1

# Numerics
numpy>=1.26.4,<3.0

# Utilities
python-dotenv>=1.1.0

# Document generation
fpdf2>=2.8.5

# Structured logging
structlog>=25.5.0

# Testing
pytest>=9.0.2
pytest-asyncio>=1.3.0
kivy>=2.3.1
