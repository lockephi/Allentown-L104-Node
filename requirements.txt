# ═══════════════════════════════════════════════════════════
# L104 Sovereign Node — Python Dependencies v2.0 (Feb 2026)
# UPGRADE v2.0:
#   - Removed Flask (FastAPI is the sole web framework)
#   - Removed duplicate JSON libs (kept orjson, dropped ujson)
#   - Moved optional deps to requirements-optional.txt
#   - Added pytest + httpx test deps
#   - Pinned both floor and ceiling versions
# ═══════════════════════════════════════════════════════════

# Web framework & ASGI
fastapi>=0.115.7,<1.0
uvicorn[standard]>=0.34.2,<1.0
pydantic>=2.12.1,<3.0
jinja2>=3.1.6,<4.0
python-multipart>=0.0.20

# HTTP client (HTTP/2 enabled)
httpx[http2]>=0.29.2,<1.0

# AI providers
anthropic>=0.50.2,<1.0
google-genai>=1.8.0,<2.0

# System monitoring
psutil>=6.2.1

# Performance: async event loop + fast JSON
uvloop>=0.22.0;sys_platform!='win32'
orjson>=3.10.16

# Networking
websockets>=15.0,<16.0

# Numerics
numpy>=1.26.4,<3.0

# Utilities
python-dotenv>=1.1.0

# Document generation
fpdf2>=2.10.0

# Structured logging
structlog>=25.0.0

# Testing
pytest>=8.3.5
pytest-asyncio>=0.25.4
kivy>=2.3.1
